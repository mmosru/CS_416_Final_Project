<%= form_for(sighting) do |f| %>
  <% if sighting.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sighting.errors.count, "error") %> prohibited this sighting from being saved:</h2>

      <ul>
      <% sighting.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :location %>
    <%= f.text_field :location %>
  </div>

  <div class="field">
    <%= f.label :observe_tm %>
    <%= f.datetime_select :observe_tm %>
  </div>

  <div class="field">
    <%= f.label :notes %>
    <%= f.text_field :notes %>
  </div>
  

<% @specieslist = Species.all %>

<%= f.label :species_id %>
<%= f.collection_select :species_id, @specieslist, :id, :common_name %>


  
  
    <%= f.hidden_field :user_id, :value => session[:user_id]%>
 

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
